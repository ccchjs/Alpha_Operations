# Add project specific ProGuard rules here.
# This app ships with isMinifyEnabled = false by default (see app/build.gradle.kts),
# so this file is a no-op until you turn minification on for release builds.
